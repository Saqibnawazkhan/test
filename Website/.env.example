# Base URL of the FastAPI backend. For local dev the backend runs on :8000.
# For the deployed Vercel site, set this to your PUBLIC backend URL (e.g. https://taxnet-api.onrender.com)
VITE_API_URL=http://localhost:8000
