# FastAPI serving layer (backend/main.py) — light, no ML.
# This is all the deployed API needs to serve every endpoint EXCEPT /person/{cnic}/explain.
fastapi>=0.111
uvicorn[standard]>=0.30
psycopg[binary]>=3.2
psycopg-pool>=3.2
anthropic>=0.39
fpdf2>=2.7
